import React from 'react';
import { VoiceHome } from './VoiceHome';

/**
 * Dedicated VoicePayment route matching project folder specifications.
 * Renders the accessible voice payment interface.
 */
export const VoicePayment: React.FC = () => {
  return <VoiceHome />;
};
